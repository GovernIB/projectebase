<?xml version="1.0" encoding="UTF-8" ?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>



  <groupId>org.fundaciobit.projectebase.archetype</groupId>
  <artifactId>projectebasearchetype-ear</artifactId>  
  <version>1.0.0</version>
  <packaging>ear</packaging>

  <parent>
    <groupId>org.fundaciobit.projectebase.archetype</groupId>
    <artifactId>projectebasearchetype</artifactId>
    <version>1.0.0</version>
  </parent>

  <name>ProjecteBaseArchetype Ear</name>


  <properties>
    <clientcert.properties.dir>./src/main/application/APP-INF/classes/</clientcert.properties.dir>
    <clientcert.properties.filename>clientcert.properties</clientcert.properties.filename>
    <clientcert.properties>${clientcert.properties.dir}${clientcert_properties_filename}</clientcert.properties>

    <timestamp>${maven.build.timestamp}</timestamp>
    <maven.build.timestamp.format>yyyy-MM-dd HH:mm</maven.build.timestamp.format>
  </properties>

  <dependencies>

    <!-- Internes -->

    <dependency>
      <groupId>org.fundaciobit.projectebase.archetype</groupId>
      <artifactId>projectebasearchetype-jpa</artifactId>
      <version>${project.version}</version>			
    </dependency>

    <dependency>
      <groupId>org.fundaciobit.projectebase.archetype</groupId>
      <artifactId>projectebasearchetype-ejb</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>

    <dependency>
      <groupId>org.fundaciobit.projectebase.archetype</groupId>
      <artifactId>projectebasearchetype-logic</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>

    <dependency>
      <groupId>org.fundaciobit.projectebase.archetype</groupId>
      <artifactId>projectebasearchetype-persistence-jta</artifactId>
      <version>${project.version}</version>
      <type>jar</type>
    </dependency>


    <dependency>
      <groupId>org.fundaciobit.projectebase.archetype</groupId>
      <artifactId>projectebasearchetype-back</artifactId>
      <version>${project.version}</version>
      <type>war</type>
    </dependency>

    <!-- Externes -->



    <!-- Commons Dependencies -->

    <dependency>
      <groupId>commons-fileupload</groupId>
      <artifactId>commons-fileupload</artifactId>
      <version>1.3</version>
    </dependency>

    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>2.2</version>
    </dependency>

    <dependency>
      <groupId>apache-lang</groupId>
      <artifactId>commons-lang</artifactId>
      <version>2.0</version>
    </dependency>

    <dependency>
      <groupId>commons-logging</groupId>
      <artifactId>commons-logging</artifactId>
      <version>1.1.1</version>
    </dependency>

    <dependency>
      <groupId>commons-digester</groupId>
      <artifactId>commons-digester</artifactId>
      <version>2.0</version>
    </dependency>

    <dependency>
      <groupId>cglib</groupId>
      <artifactId>cglib</artifactId>
      <version>2.2.2</version>
    </dependency>

    <!-- =====================================================  -->
    <!-- Necessitam les llibreries d'AXIS ja que ho requereix   -->
    <!-- el plugin de CERTIFICAT-AFIRMA i el Plugin de CUSTODIA -->
    <!-- CAIB. Hem d'excloure el jar de axis-jaxrpc ja que      -->
    <!-- conté la classe QNAME que el JBoss no accepta i per    -->
    <!-- això es substitueix per axis-jaxrpc-without-qname que  -->
    <!-- no la conté.                                           -->
    <!-- ====================================================== -->
    <!--
		<dependency>
			<groupId>axis</groupId>
			<artifactId>axis</artifactId>
			<version>1.4</version>
			
			<exclusions>
				<exclusion>
					<groupId>axis</groupId>
					<artifactId>axis-jaxrpc</artifactId>
				</exclusion>
			 </exclusions> 
		</dependency>
		
		<dependency>
			<groupId>axis</groupId>
			<artifactId>axis-jaxrpc-without-qname</artifactId>
			<version>1.3</version>
		</dependency>
		-->


    <!-- WS de ProjecteBaseArchetype -->
    <!--  http://localhost:8080/projectebasearchetype/ws/v1/ProjecteBaseArchetypeHelloWorld -->
    <!--  http://localhost:8080/projectebasearchetype/ws/v1/ProjecteBaseArchetypeHelloWorldWithSecurity -->
    <!--  ... -->
    <dependency>
      <groupId>org.fundaciobit.projectebase.archetype</groupId>
      <artifactId>projectebasearchetype-ws-server</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>
    

  </dependencies>



  <build>
    <finalName>projectebasearchetype</finalName>

    <filters>
      <filter>../config.properties</filter>
    </filters>


    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>1.6</source>
            <target>1.6</target>
            <encoding>UTF-8</encoding>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>


    <plugins>


      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <version>2.5</version>
        <!-- Aquest bloc força a netejar sempre -->
        <executions>
          <execution>
            <id>auto-clean</id>
            <phase>initialize</phase>
            <goals>
              <goal>clean</goal>
            </goals>
          </execution>
        </executions>
      </plugin>


      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-ear-plugin</artifactId>
        <version>2.3.2</version>
        <!-- configuring the ear plugin -->
        <configuration>

          <displayName>ProjecteBaseArchetype</displayName>
          <description>ProjecteBaseArchetype</description>

          <archive>
            <manifest>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
            </manifest>
            <manifestEntries>
              <ProjecteBaseArchetype-Version>${project.version}</ProjecteBaseArchetype-Version>
              <ProjecteBaseArchetype-Build-Date>${timestamp}</ProjecteBaseArchetype-Build-Date>
            </manifestEntries>
          </archive>

          <defaultLibBundleDir>lib</defaultLibBundleDir>
          <!--
		  <packagingExcludes>lib/axis-jaxrpc-1.4.jar</packagingExcludes>
-->
          <jboss>
            <version>5</version>
            <loader-repository>org.fundaciobit.projectebase.archetype:loader=projectebasearchetype.ear</loader-repository>
            <loader-repository-config>java2ParentDelegation=false</loader-repository-config>
            <!--<security-domain>java:/jaas/seycon</security-domain>-->
          </jboss>

          <modules>

            <webModule>
              <groupId>org.fundaciobit.projectebase.archetype</groupId>
              <artifactId>projectebasearchetype-back</artifactId>
              <contextRoot>projectebasearchetype</contextRoot>
            </webModule>

            <ejbModule>
              <groupId>org.fundaciobit.projectebase.archetype</groupId>
              <artifactId>projectebasearchetype-ejb</artifactId>
            </ejbModule>

            <ejbModule>
              <groupId>org.fundaciobit.projectebase.archetype</groupId>
              <artifactId>projectebasearchetype-logic</artifactId>
            </ejbModule>

            <jarModule>
              <groupId>org.fundaciobit.projectebase.archetype</groupId>
              <artifactId>projectebasearchetype-persistence-jta</artifactId>
            </jarModule>

            <ejbModule>
              <groupId>org.fundaciobit.projectebase.archetype</groupId>
              <artifactId>projectebasearchetype-ws-server</artifactId>
            </ejbModule>
          </modules>

        </configuration>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>jboss-maven-plugin</artifactId>
        <version>1.5.0</version>
        <configuration>
          <!-- <jbossHome>D:\servers\jboss-eap-5.1\jboss-as</jbossHome>-->
          <serverName>default</serverName>
          <fileName>target/projectebasearchetype.ear</fileName> 
        </configuration>
      </plugin>

    </plugins>
  </build>

</project>
