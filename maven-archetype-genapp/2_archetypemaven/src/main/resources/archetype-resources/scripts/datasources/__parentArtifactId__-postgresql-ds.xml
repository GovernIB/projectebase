#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
﻿<?xml version="1.0" encoding="UTF-8" ?>
<datasources>
    <local-tx-datasource>
        <jndi-name>${package}.db</jndi-name>
        <connection-url>jdbc:postgresql://localhost:5432/${parentArtifactId}</connection-url>
        <driver-class>org.postgresql.Driver</driver-class>
        <user-name>${parentArtifactId}</user-name>
        <password>${parentArtifactId}</password>
    </local-tx-datasource>
</datasources>