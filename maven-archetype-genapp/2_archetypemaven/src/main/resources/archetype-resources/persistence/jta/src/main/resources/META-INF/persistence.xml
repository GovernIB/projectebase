#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8" ?>

<persistence version="1.0"
        xmlns="http://java.sun.com/xml/ns/persistence"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://java.sun.com/xml/ns/persistence
http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd">
    <persistence-unit name="${artifactId}DB" transaction-type="JTA">
        <provider>org.hibernate.ejb.HibernatePersistence</provider>
        <jta-data-source>java:/${package}.db</jta-data-source>		
		
		<class>${package}.jpa.FitxerJPA</class>
		<class>${package}.jpa.IdiomaJPA</class>
		<class>${package}.jpa.TraduccioJPA</class>
		<class>${package}.jpa.TraduccioMapJPA</class>

        <properties>
            <property name="hibernate.dialect" value="${symbol_dollar}{${package}.hibernate.dialect}" />
            <property name="hibernate.query.substitutions" value="${symbol_dollar}{${package}.hibernate.query.substitutions}"/>
        </properties>
    </persistence-unit>
</persistence>