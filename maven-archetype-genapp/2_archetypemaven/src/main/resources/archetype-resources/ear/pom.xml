<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>${groupId}</groupId>
  <artifactId>${artifactId}</artifactId>  
  <version>${version}</version>
  <packaging>ear</packaging>

  <parent>
    <groupId>${groupId}</groupId>
    <artifactId>${rootArtifactId}</artifactId>
    <version>${version}</version>
  </parent>

  <name>${projectname} Ear</name>


  <properties>
    <clientcert.properties.dir>./src/main/application/APP-INF/classes/</clientcert.properties.dir>
    <clientcert.properties.filename>clientcert.properties</clientcert.properties.filename>
    <clientcert.properties>${clientcert.properties.dir}${clientcert_properties_filename}</clientcert.properties>

    <timestamp>${maven.build.timestamp}</timestamp>
    <maven.build.timestamp.format>yyyy-MM-dd HH:mm</maven.build.timestamp.format>
  </properties>

  <dependencies>

    <!-- Internes -->

    <dependency>
      <groupId>${groupId}</groupId>
      <artifactId>${rootArtifactId}-jpa</artifactId>
      <version>${version}</version>			
    </dependency>

    <dependency>
      <groupId>${groupId}</groupId>
      <artifactId>${rootArtifactId}-ejb</artifactId>
      <version>${version}</version>
      <type>ejb</type>
    </dependency>

    <dependency>
      <groupId>${groupId}</groupId>
      <artifactId>${rootArtifactId}-logic</artifactId>
      <version>${version}</version>
      <type>ejb</type>
    </dependency>

    <dependency>
      <groupId>${groupId}</groupId>
      <artifactId>${rootArtifactId}-persistence-jta</artifactId>
      <version>${version}</version>
      <type>jar</type>
    </dependency>


    <dependency>
      <groupId>${groupId}</groupId>
      <artifactId>${rootArtifactId}-back</artifactId>
      <version>${version}</version>
      <type>war</type>
    </dependency>

    <!-- Externes -->



    <!-- Commons Dependencies -->

    <dependency>
      <groupId>commons-fileupload</groupId>
      <artifactId>commons-fileupload</artifactId>
      <version>1.3</version>
    </dependency>

    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>2.2</version>
    </dependency>

    <dependency>
      <groupId>apache-lang</groupId>
      <artifactId>commons-lang</artifactId>
      <version>2.0</version>
    </dependency>

    <dependency>
      <groupId>commons-logging</groupId>
      <artifactId>commons-logging</artifactId>
      <version>1.1.1</version>
    </dependency>

    <dependency>
      <groupId>commons-digester</groupId>
      <artifactId>commons-digester</artifactId>
      <version>2.0</version>
    </dependency>

    <dependency>
      <groupId>cglib</groupId>
      <artifactId>cglib</artifactId>
      <version>2.2.2</version>
    </dependency>

    <!-- =====================================================  -->
    <!-- Necessitam les llibreries d'AXIS ja que ho requereix   -->
    <!-- el plugin de CERTIFICAT-AFIRMA i el Plugin de CUSTODIA -->
    <!-- CAIB. Hem d'excloure el jar de axis-jaxrpc ja que      -->
    <!-- conté la classe QNAME que el JBoss no accepta i per    -->
    <!-- això es substitueix per axis-jaxrpc-without-qname que  -->
    <!-- no la conté.                                           -->
    <!-- ====================================================== -->
    <!--
		<dependency>
			<groupId>axis</groupId>
			<artifactId>axis</artifactId>
			<version>1.4</version>
			
			<exclusions>
				<exclusion>
					<groupId>axis</groupId>
					<artifactId>axis-jaxrpc</artifactId>
				</exclusion>
			 </exclusions> 
		</dependency>
		
		<dependency>
			<groupId>axis</groupId>
			<artifactId>axis-jaxrpc-without-qname</artifactId>
			<version>1.3</version>
		</dependency>
		-->


    <!-- WS START -->  
    <!-- WS de ${projectname} -->
    <!--  http://localhost:8080/${artifactId}/ws/v1/${projectname}HelloWorld -->
    <!--  http://localhost:8080/${artifactId}/ws/v1/${projectname}HelloWorldWithSecurity -->
    <!--  ... -->
    <dependency>
      <groupId>${groupId}</groupId>
      <artifactId>${rootArtifactId}-ws-server</artifactId>
      <version>${version}</version>
      <type>ejb</type>
    </dependency>
    <!-- WS END -->

  </dependencies>



  <build>
    <finalName>${artifactId}</finalName>

    <filters>
      <filter>../config.properties</filter>
    </filters>


    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>1.6</source>
            <target>1.6</target>
            <encoding>UTF-8</encoding>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>


    <plugins>


      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <version>2.5</version>
        <!-- Aquest bloc força a netejar sempre -->
        <executions>
          <execution>
            <id>auto-clean</id>
            <phase>initialize</phase>
            <goals>
              <goal>clean</goal>
            </goals>
          </execution>
        </executions>
      </plugin>


      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-ear-plugin</artifactId>
        <version>2.3.2</version>
        <!-- configuring the ear plugin -->
        <configuration>

          <displayName>${projectname}</displayName>
          <description>${projectname}</description>

          <archive>
            <manifest>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
            </manifest>
            <manifestEntries>
              <${projectname}-Version>${project.version}</${projectname}-Version>
              <${projectname}-Build-Date>${timestamp}</${projectname}-Build-Date>
            </manifestEntries>
          </archive>

          <defaultLibBundleDir>lib</defaultLibBundleDir>
          <!--
		  <packagingExcludes>lib/axis-jaxrpc-1.4.jar</packagingExcludes>
-->
          <jboss>
            <version>5</version>
            <loader-repository>${package}:loader=${artifactId}.ear</loader-repository>
            <loader-repository-config>java2ParentDelegation=false</loader-repository-config>
            <!--<security-domain>java:/jaas/seycon</security-domain>-->
          </jboss>

          <modules>

            <webModule>
              <groupId>${groupId}</groupId>
              <artifactId>${rootArtifactId}-back</artifactId>
              <contextRoot>${rootArtifactId}</contextRoot>
            </webModule>

            <ejbModule>
              <groupId>${groupId}</groupId>
              <artifactId>${rootArtifactId}-ejb</artifactId>
            </ejbModule>

            <ejbModule>
              <groupId>${groupId}</groupId>
              <artifactId>${rootArtifactId}-logic</artifactId>
            </ejbModule>

            <jarModule>
              <groupId>${groupId}</groupId>
              <artifactId>${rootArtifactId}-persistence-jta</artifactId>
            </jarModule>
            <!-- WS START -->
            <ejbModule>
              <groupId>${groupId}</groupId>
              <artifactId>${rootArtifactId}-ws-server</artifactId>
            </ejbModule>
            <!-- WS END -->
          </modules>

        </configuration>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>jboss-maven-plugin</artifactId>
        <version>1.5.0</version>
        <configuration>
          <!-- <jbossHome>D:\servers\jboss-eap-5.1\jboss-as</jbossHome>-->
          <serverName>default</serverName>
          <fileName>target/${artifactId}.ear</fileName> 
        </configuration>
      </plugin>

    </plugins>
  </build>

</project>
