<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <!-- TODO: l'estàndard hauria de definir un nom per el name del persistence unit? -->
    <persistence-unit name="projectebasePU" transaction-type="JTA">

        <jta-data-source>java:/jdbc/projectebaseDS</jta-data-source>

        <class>es.caib.projectebase.jpa.UnitatOrganica</class>

        <properties>
            <!-- Per les propietats estandard veure punt 8.2.1.9 de l'especificació JPA -->
            <!-- TODO: Aquesta opció mai s'ha d'emprar en entorn que no sigui de proves -->
            <property name="javax.persistence.schema-generation.database.action" value="drop-and-create"/>

            <property name="hibernate.show_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
