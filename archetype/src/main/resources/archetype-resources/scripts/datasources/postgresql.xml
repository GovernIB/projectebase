#set( $symbol_pount = '#' )
#set( $symbol_escape = '\' )
#set( $symbol_dollar = '$' )
<datasource jndi-name="java:jboss/datasources/${rootArtifactId}DS" pool-name="${rootArtifactId}DS" enabled="true"
            use-java-context="true">
    <connection-url>jdbc:postgresql://localhost:5432/${rootArtifactId}</connection-url>
    <driver>postgresql</driver>
    <security>
        <user-name>${rootArtifactId}</user-name>
        <password>${rootArtifactId}</password>
    </security>
</datasource>