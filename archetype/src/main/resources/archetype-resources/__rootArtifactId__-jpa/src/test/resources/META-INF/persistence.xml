#set( $symbol_pount = '#' )
#set( $symbol_escape = '\' )
#set( $symbol_dollar = '$' )
<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <persistence-unit name="testPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.ejb.HibernatePersistence</provider>
        <!-- Llista de classes persistents -->
        <class>${package}.jpa.UnitatOrganica</class>
        <class>${package}.jpa.Procediment</class>
        <!-- Activar cache només per aquelles entitats on volguem expressament -->
        <shared-cache-mode>ENABLE_SELECTIVE</shared-cache-mode>
        <!-- Sempre activar la validació de les propietats -->
        <validation-mode>CALLBACK</validation-mode>
        <properties>
            <property name="javax.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/${artifactId}" />
            <property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="javax.persistence.jdbc.user" value="${artifactId}"/>
            <property name="javax.persistence.jdbc.password" value="${artifactId}"/>

            <!-- Per les propietats estandard veure punt 8.2.1.9 de l'especificació JPA -->
            <!-- Recrea automàticament la BDD. Aquesta opció MAI s'ha d'emprar en entorn que no sigui de proves -->
            <property name="javax.persistence.schema-generation.database.action" value="drop-and-create"/>
            <!--
            Propietats específiques de Hibernate
            https://docs.jboss.org/hibernate/orm/5.3/userguide/html_single/Hibernate_User_Guide.html${symbol_pound}configurations
            -->
            <property name="hibernate.show_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>